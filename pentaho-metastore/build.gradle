dependencies {
    compile(
            libraries.'commons-codec',
            libraries.'commons-io',
            libraries.'guava',
    )
    testCompile (
            libraries.'mockito-all',
    )
}
